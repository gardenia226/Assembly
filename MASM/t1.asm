.386
DATA SEGMENT USE16
  CH1 DB 'ABCDEF',00H
  NUM DB ?
  BUF DB 'The number of char is£º$'
DATA ENDS

CODE SEGMENT USE16
  ASSUME CS:CODE,DS:DATA
START:
  MOV AX,DATA
  MOV DS,AX
  XOR DX,DX
  XOR BX,BX
LOOP1:
  MOV CH,0
  MOV CL,[BX]
  JCXZ LOOP2
  INC DX
  INC BX
  JMP LOOP1
LOOP2:
     MOV AX,4C00H
     INT 21H
CODE ENDS
     END START

